# ==============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================================================
# Copy this file to .env.production and fill in actual values
# DO NOT commit .env.production to version control
# ==============================================================================

# ==== 🌍 Environment ====
NODE_ENV=production
PORT=5000

# ==== 🔐 Google OAuth (REQUIRED) ====
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_production_google_client_id
GOOGLE_CLIENT_SECRET=your_production_google_client_secret
GOOGLE_REDIRECT_URI=https://processsutra.com/api/auth/google/callback

# Frontend Google OAuth (must match backend)
VITE_GOOGLE_CLIENT_ID=your_production_google_client_id

# ==== 🗄️ Database - PostgreSQL (REQUIRED) ====
# Production PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Example: postgresql://user:pass@db.example.com:5432/processsutra
DATABASE_URL=postgresql://username:password@production-host:5432/processsutra

# ==== 🍃 Database - MongoDB (REQUIRED) ====
# Production MongoDB connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
# Example: mongodb+srv://user:pass@cluster0.mongodb.net/processsutra
MONGODB_URI=mongodb+srv://username:password@production-cluster.mongodb.net/processsutra
MONGODB_DB=processsutra

# ==== 🔑 Session Secret (REQUIRED) ====
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# MUST be at least 32 characters (128+ recommended for production)
SESSION_SECRET=generate_a_128_character_random_string_here

# ==== 🍪 Cookie Security (REQUIRED for Production) ====
# DO NOT set these to false in production - leave commented or set to true
COOKIE_SECURE=true
COOKIE_SAMESITE=strict
# INSECURE_COOKIES should NOT be set in production

# ==== 🔐 Encryption Key (REQUIRED) ====
# AES-256-GCM encryption key for WhatsApp & Gmail addon credentials
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# MUST be exactly 64 hex characters (32 bytes)
ENCRYPTION_KEY=generate_64_hex_character_key_here

# ==== 🌐 Domain Configuration ====
DOMAIN=processsutra.com
ALLOWED_ORIGINS=https://processsutra.com,https://www.processsutra.com

# ==== 🔒 Security ====
# Disable development authentication in production (REQUIRED)
DISABLE_DEV_AUTH=true

# ==== 📊 Optional: Monitoring & Error Tracking ====
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# LOG_LEVEL=info

# ==== 📧 Optional: Email Service (for notifications) ====
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# ==== 🔗 Optional: External Services ====
# REDIS_URL=redis://redis-host:6379
# S3_BUCKET=your-backup-bucket
# S3_REGION=us-east-1
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key

# ==============================================================================
# SECURITY CHECKLIST BEFORE PRODUCTION DEPLOYMENT
# ==============================================================================
# ✅ All credentials are production-specific (not localhost)
# ✅ SESSION_SECRET is at least 128 characters
# ✅ ENCRYPTION_KEY is exactly 64 hex characters
# ✅ COOKIE_SECURE=true (or not set, defaults to true in production)
# ✅ DISABLE_DEV_AUTH=true
# ✅ Google OAuth redirect URI uses https:// not http://
# ✅ Database URLs use production hosts (not localhost)
# ✅ This file is in .gitignore
# ✅ Credentials have been rotated if previously exposed
# ==============================================================================
